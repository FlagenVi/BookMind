# Суть текста — frontend

React + TypeScript + Vite + Tailwind CSS.

```powershell
npm ci
npm run dev
```

Проверки: `npm run build`, `npm run lint`, `npm run format:check`.
Форматирование: `npm run format`.

Инструкции по backend, API, текущим ограничениям и структуре: [README проекта](../../README.md).
